import * as React from "react";
interface TransparentBoxProps {
    children: React.ReactNode;
    padding?: string;
    className?: string;
}
declare const TransparentBox: React.FC<TransparentBoxProps>;
export default TransparentBox;
